#labels Featured,Phase-Support
 # Open cachegrind file in CachegrindVisualizer (it creates xdebug).
 # In the same directory as the source file cachegrind, find the file with the same name as the original file, but with the extension of dot and open it in ZGRViewer.